# Mission Complete!

You've learned that `docker stop` isn't just a power button—it's a polite request.
Using `STOPSIGNAL` allows your application to clean up resources (close DB connections, finish requests) before exiting.
