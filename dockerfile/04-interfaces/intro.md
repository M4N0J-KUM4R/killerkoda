# Interfaces (EXPOSE & VOLUME)

Definitions:
- **EXPOSE**: Documentation. It tells humans "I listen on this port". It does NOT publish the port.
- **VOLUME**: Persistence. It tells Docker "This folder contains data. Never delete it."
