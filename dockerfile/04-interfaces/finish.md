# Mission Complete!

**Recap:**
- `EXPOSE`: Just metadata. Use it to help other developers know which port to map.
- `VOLUME`: Tells Docker "Keep this folder safe". It prevents accidental data loss when a container is removed.
