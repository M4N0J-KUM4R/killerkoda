# Mission Complete!

**Recap:**
- Use `ARG` for passing versions (`NODE_VERSION`, `maven_version`) during build.
- Use `ENV` for software configuration (`DB_HOST`, `DEBUG_MODE`) that the application needs while running.
