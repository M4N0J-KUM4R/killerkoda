# Mission Complete!

**Recap:**
- `COPY`: Simple, predictable, secure. **Use this by default.**
- `ADD`: Extracts archives and can download URLs (though `curl` is better for that). Use it only when you *want* auto-extraction.
