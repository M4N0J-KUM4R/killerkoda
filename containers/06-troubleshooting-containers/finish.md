# Case Closed.

You have learned the Detective's Toolkit:
-   `docker logs`: For post-mortem analysis.
-   `docker inspect`: For checking the blueprints (configuration).
-   `docker exec`: For performing a live raid (interactive debugging).

You have completed the **Containers Module**!
You started knowing nothing, and now you can run, manage, and debug containers like a pro.

Next Module: **Images** (Building your own artifacts).
