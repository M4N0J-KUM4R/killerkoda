# Mission Complete!

You now understand that `docker tag` creates a new name for an existing image ID.
`docker rmi` removes a tag. The image data is only deleted when **no tags** point to it anymore.
