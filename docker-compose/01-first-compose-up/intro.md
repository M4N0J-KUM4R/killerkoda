# One File to Rule Them All

`docker run` is great for one container.
But what if you need a Web Server, a Database, and a Cache all connected together?

Running 3 commands every time is painful.
**Docker Compose** lets you define them all in `docker-compose.yaml` and start them with ONE command.
