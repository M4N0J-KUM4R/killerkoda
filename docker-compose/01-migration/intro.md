# The Migration

Stop writing shell scripts with 10 lines of `docker run ...`.
**Docker Compose** is a tool for defining and running multi-container applications.
It uses a YAML file to configure your application's services.
